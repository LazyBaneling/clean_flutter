name: clean_flutter
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  clean_framework: ^2.0.10
  clean_framework_router: ^0.4.1
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^5.0.2
  url_launcher: ^6.1.10
  mocktail: ^0.3.0

dev_dependencies:
  clean_framework_test:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  coverage: ^1.6.3

flutter:
  uses-material-design: true
